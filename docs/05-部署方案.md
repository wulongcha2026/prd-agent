# PRD 生成 Agent - 部署方案

> 项目版本：v1.0 | 编制人： | 日期：2026-XX-XX

---

## 1. 部署架构

### 1.1 系统架构

```
                          ┌──────────────────────┐
                          │     负载均衡器        │
                          └──────────┬───────────┘
                                     │
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
              ▼                      ▼                      ▼
    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
    │  API 实例 1    │    │  API 实例 2    │    │  API 实例 N    │
    │  (容器/函数)   │    │  (容器/函数)   │    │  (容器/函数)   │
    └────────┬───────┘    └────────┬───────┘    └────────┬───────┘
              │                      │                      │
              └──────────────────────┼──────────────────────┘
                                     │
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
              ▼                      ▼                      ▼
    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
    │   Redis         │    │    LLM API     │    │    日志服务    │
    │   (状态存储)    │    │  (DashScope)   │    │   (日志)       │
    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 1.2 部署模式

| 模式 | 适用场景 | 成本 | 复杂度 |
|------|----------|------|--------|
| **Docker Compose** | 开发/测试 | 低 | 简单 |
| **K8s** | 生产环境 | 中 | 中等 |
| **Serverless** | 按量使用 | 按量 | 简单 |

---

## 2. Docker Compose 部署

### 2.1 docker-compose.yml

```yaml
version: '3.8'

services:
  prd-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}
      - LLM_MODEL=qwen-max
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
```

### 2.2 Dockerfile

```dockerfile
FROM python:3.10-slim

WORKDIR /app

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2.3 启动命令

```bash
# 1. 克隆项目
git clone https://github.com/wulongcha2026/prd-agent.git
cd prd-agent

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env，填入 DASHSCOPE_API_KEY

# 3. 启动服务
docker-compose up -d

# 4. 测试
curl http://localhost:8000/health
```

---

## 3. 阿里云函数计算部署

### 3.1 创建函数

1. 登录阿里云函数计算控制台
2. 创建服务：`prd-agent`
3. 创建函数：HTTP 函数
4. 配置：
   - 运行环境：Python 3.10
   - 内存：1024 MB
   - 超时：60 秒

### 3.2 环境变量

| 变量名 | 值 |
|--------|-----|
| DASHSCOPE_API_KEY | 阿里云 API Key |
| LLM_MODEL | qwen-max |

---

## 4. 部署检查清单

### 4.1 部署前

| 检查项 | 状态 |
|--------|------|
| API Key 已配置 | ☐ |
| 依赖已安装 | ☐ |
| 代码已测试 | ☐ |

### 4.2 部署后

| 检查项 | 验证方式 |
|--------|----------|
| 服务可访问 | `curl /health` |
| 生成功能正常 | 测试生成 PRD |
| 响应时间 < 30s | 实际测试 |

---

## 5. 监控与运维

### 5.1 监控指标

| 指标 | 告警阈值 |
|------|----------|
| 请求量 | QPS > 50 |
| 响应时间 | P99 > 30s |
| 错误率 | > 5% |

### 5.2 日志

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

---

> 📝 **备注**：本文档为部署方案模板。